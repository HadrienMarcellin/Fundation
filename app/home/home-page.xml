<Page navigatingTo="onNavigatingTo" xmlns="http://schemas.nativescript.org/tns.xsd">
    
    <ActionBar>
        <Label text="Home" />
    </ActionBar>
    
    <StackLayout height="100%">
        <ListView height="100%" separatorColor="transparent" items="{{ buttons }}" itemTap="{{ onButtonTap }}">
            <ListView.itemTemplate>
                <Button text="{{ text }}" />
            </ListView.itemTemplate>
        </ListView>
    </StackLayout>
    <!--
    <StackLayout orientation=" vertical">
        <label>Foundation</label>
        <button text=" New Game" tap=" onTapNewGame"></button>
        <button text=" Settings" tap=" onTapSettings"></button>
        <button text=" Profile" tap=" onTapProfile"></button>
    </StackLayout>
    -->
</Page>